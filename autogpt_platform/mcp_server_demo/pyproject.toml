[tool.poetry]
name = "autogpt-mcp-demo"
version = "0.1.0"
description = "Demo MCP server for AutoGPT platform"
authors = ["AutoGPT <devnull@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
mcp = "*"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
